/**
 * Background placeholder.
 *
 * The body itself paints `canvas-dark` (`#000000`) — see `index.css`.
 * No overlay, gradient, or grid pattern. The Revolut language treats the
 * canvas as a flat surface that gets activated by content, not by chrome.
 */
export function Background() {
  return null;
}
